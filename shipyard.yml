base: 'ubuntu/trusty'

workdir: /code

python27:
  requirements:
    - requirements.txt

npm:
    - grunt

add:
  docs/: /some-docs-folder
  requirements.txt: /code/requirements.txt

after_install:
  - cp /build/git/app.py /code/

cmd: python app.py
